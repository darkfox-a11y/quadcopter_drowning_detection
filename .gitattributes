# Git LFS rules intentionally minimized because the repository is now kept
# lightweight for GitHub. Large raw videos, full datasets, and model weights
# are stored locally and documented in the README instead of being tracked.
